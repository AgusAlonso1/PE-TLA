function fun(a: number) {
  return a+2
}
let b=2
fun(b)
let c = fun(3)

async function callAsyncFunction() {
  const result = await asyncFunctionName(5)
}

async function asyncFunctionName(a: number) {
  return a+2
}

callAsyncFunction()

